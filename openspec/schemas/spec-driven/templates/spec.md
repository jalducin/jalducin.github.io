## Purpose
<!-- Solo para capabilities NUEVAS: 1-3 líneas en español que expliquen qué garantiza esta capability y para
quién. Se copia tal cual al spec.md principal (openspec/specs/<capability>/spec.md) al archivar; los specs
principales requieren "## Purpose" + "## Requirements" (openspec validate --specs) y no cambia en deltas de
capabilities existentes, que solo agregan/modifican/eliminan requirements. -->

## ADDED Requirements

### Requirement: <!-- requirement name -->
<!-- requirement text: usa SHALL o MUST, no "debe" -->

#### Scenario: <!-- scenario name -->
- **WHEN** <!-- condition -->
- **THEN** <!-- expected outcome -->
